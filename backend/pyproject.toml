[project]
name = "nextusinger"
version = "0.1.0"
description = "Prototype singing voice studio with DiffSinger-compatible adapters"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "NextUSinger contributors" }]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
